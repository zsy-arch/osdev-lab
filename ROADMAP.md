# ROADMAP

从入门到精通的完整路线。每个 Lab 标了预计投入时间（假设你每天能连续写 2~4 小时代码，且看完对应文档）、难度、以及"做完这个 Lab 你能回答什么问题"。时间是参考值，第一次接触 OS 开发普遍会比预计慢，这正常。

## 里程碑总览

| 里程碑 | 覆盖 Lab | 你将获得的能力 |
|---|---|---|
| M1 能跑起来 | Lab0-Lab2 | 装好工具链，让自己写的代码在 QEMU 里第一次打印字符 |
| M2 管住内存 | Lab3-Lab4 | 手写物理页分配器和页表，理解虚拟地址是怎么变成物理地址的 |
| M3 响应世界 | Lab5-Lab6 | 内核能被打断（中断/异常），能接住用户态的系统调用请求 |
| M4 像真的操作系统 | Lab7-Lab8 | 多进程并发运行、有文件系统 |
| M5 收尾与进阶 | Lab9-Lab10 | 能跑用户程序、理解多核和锁 |

## Sub Lab 列表

### Lab0：环境、工具链、QEMU、构建系统
- **难度**：★☆☆☆☆
- **预计时间**：0.5~1 天（大部分时间在等下载）
- **前置**：无
- **目标**：搞定 Docker 或原生工具链，跑通 `make run`，看到最简单的 QEMU 输出。
- **产出**：一个能在你机器上正常工作的构建/运行环境。
- **常见卡点**：macOS 缺交叉编译器、WSL2 网络代理导致下载失败、Docker 内存/磁盘限额太小。

### Lab1：Boot 与裸机输出
- **难度**：★★☆☆☆
- **预计时间**：1~2 天
- **前置**：Lab0
- **目标**：x86_64 用 Multiboot2 + GRUB（可选 Limine）从 32 位保护模式跳到 64 位长模式；riscv64 用 OpenSBI 完成 M 模式到 S 模式切换。两边都做到"串口打印一行字然后停住"。
- **核心概念**：固件/引导器职责边界、实模式→保护模式→长模式（x86）、SBI 调用约定（riscv）、串口 UART 寄存器编程。

### Lab2：内核入口、链接脚本、BSS、栈、panic
- **难度**：★★☆☆☆
- **预计时间**：1~2 天
- **前置**：Lab1
- **目标**：写链接脚本控制内核在内存里的布局，在 C 代码跑之前手动清零 BSS、设置好栈指针，做一个能打印文件名+行号然后死循环的 `panic()`。
- **核心概念**：ELF 段（`.text`/`.data`/`.bss`）、链接脚本 `SECTIONS`、栈帧、符号表用于调试。

### Lab3：物理内存管理
- **难度**：★★★☆☆
- **预计时间**：2~3 天
- **前置**：Lab2
- **目标**：从固件拿到可用内存范围（x86 用 Multiboot2 mmap tag，riscv 用 OpenSBI/DTB），写一个链表式空闲页分配器（`kalloc`/`kfree`），做到能分配/释放任意数量的 4KiB 页且不重叠不泄漏。
- **核心概念**：物理地址空间布局、E820/Multiboot mmap、Device Tree 基础、空闲链表分配器。

### Lab4：虚拟内存与页表
- **难度**：★★★★☆（第一个真正难的 Lab）
- **预计时间**：3~5 天
- **前置**：Lab3
- **目标**：x86_64 实现 4 级页表（PML4，可选 PML5 讨论），riscv64 实现 Sv39（可选 Sv48 讨论）。做到内核态自映射、身份映射、以及一个手写的"映射一段虚拟地址到指定物理地址"函数，触发并正确处理一次缺页异常。
- **核心概念**：多级页表遍历、页表项权限位、TLB 与 `invlpg`/`sfence.vma`、缺页异常的地址来源（`CR2` / `stval`）。

### Lab5：中断、异常、trap、时钟
- **难度**：★★★★☆
- **预计时间**：3~4 天
- **前置**：Lab4
- **目标**：x86_64 搭 GDT + IDT，处理时钟中断（PIT 起步，讨论 APIC）；riscv64 设置 `stvec`，处理 `scause` 分类的异常/中断，接入 CLINT `mtime` 时钟。两边都做到"每秒打印一次 tick"。
- **核心概念**：trap 帧保存/恢复、中断向量表、优先级与屏蔽、时钟中断驱动的抢占基础。

### Lab6：用户态与系统调用
- **难度**：★★★★★
- **预计时间**：4~6 天（心理门槛最高的一课：第一次真正切特权级）
- **前置**：Lab5
- **目标**：构造一个用户态页表和执行环境，x86_64 用 `syscall`/`sysret`（讨论 `int 0x80` 作为对照），riscv64 用 `ecall`，实现参数传递（`a0-a5`/寄存器约定）、内核态到用户态往返、`sys_write` 一类最小系统调用。
- **核心概念**：特权级切换的完整路径（保存上下文→提权→跳转→用户执行→trap 回内核→恢复上下文）、系统调用号约定、内核栈与用户栈切换。

### Lab7：进程与调度
- **难度**：★★★★☆
- **预计时间**：4~5 天
- **前置**：Lab6
- **目标**：进程控制块、上下文切换（`swtch`），实现 `fork`/`exec`/`wait`/`exit`，一个简单的轮转调度器。
- **核心概念**：内核栈布局与上下文切换汇编、进程状态机、僵尸进程回收。

### Lab8：简单文件系统与块设备
- **难度**：★★★★☆
- **预计时间**：3~5 天
- **前置**：Lab7
- **目标**：一个基于内存/虚拟磁盘镜像的块设备驱动，xv6 风格的简化 inode 文件系统（超级块、inode 表、位图、目录项）。
- **核心概念**：块设备抽象、inode 与数据块映射、日志/一致性的取舍（本课简化，讨论但不强制实现完整 journaling）。

### Lab9：Shell 与用户程序
- **难度**：★★★☆☆
- **预计时间**：3~4 天
- **前置**：Lab8
- **目标**：ELF 加载器、极简 libc（`write`/`exit`/`fork`/`exec` 包装），一个能跑 `ls`/`echo`/管道的 shell。
- **核心概念**：ELF program header 解析、用户程序加载到新地址空间、简单管道 IPC。

### Lab10：SMP、锁、高级主题、收尾对比
- **难度**：★★★★★
- **预计时间**：5+ 天，开放式
- **前置**：Lab9
- **目标**：x86_64 用 ACPI MADT + APIC 启动其他核心，riscv64 用 SBI HSM 扩展启动其他 hart；实现自旋锁，讨论内存序；最后一节做 x86_64 与 riscv64 全课程的架构对照复盘。
- **核心概念**：多核启动协议、原子操作、锁与内存屏障、Per-CPU 数据。

## 可选分支

- **Rust 分支**：每个 Lab 完成 C 版本后，可选择性地把该 Lab 用 Rust 重写一遍，参考 [`docs/rust-track.md`](docs/rust-track.md)。不是强制路径,建议先把 C 主线走完一遍再回头做 Rust 迁移,这样你在对比"语言帮你避免了什么错误"时才有参照。
- **Limine 分支**：Lab1 默认给 Multiboot2 + GRUB，文档里同时给出 Limine 版本作为对照，二者任选其一继续后续 Lab。
- **SMP 提前实验**：如果你对 Lab10 的多核话题特别感兴趣,可以在完成 Lab6 之后先跳读 Lab10 的启动部分（不依赖 Lab7-9 的进程/文件系统内容）。

## 建议节奏

全职学习（每天 4~6 小时）：约 5~6 周做完 Lab0-Lab9,Lab10 再单独留 1~2 周。
业余学习（每周 5~8 小时）：约 4~5 个月完整走完。

不建议跳课。Lab3 之后每个 Lab 的 solution 都建立在前一个 Lab 的 solution 之上,跳过某一课直接看后面的代码会看不懂"为什么这里要这样写"。如果只是想快速看一眼某个知识点的实现,可以直接读对应 `solution/`,但不算完成这个 Lab。
