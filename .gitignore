# 构建产物：每个 Lab 的 solution/starter 各自在 <arch>/build/ 下生成，
# 全部可以由 `make ARCH=... LAB=... VARIANT=... build` 从源码重新生成，
# 不需要进版本库。`make clean` 会把下面这些全部删掉。
build/
*.o
*.elf
*.iso
*.img

# user_prog.bin 是 user_prog.S 经过链接+objcopy 之后的裸二进制，
# 由 user_blob.S 嵌进内核镶像，属于中间产物（见各 Lab Makefile 的
# clean 目标，它跟 build/ 一起被清理）。
user_prog.bin

# mkfs 是宿主工具编译产物（labs/lab08-filesystem、lab09-shell-userspace
# 下的 mkfs/mkfs.c 用 HOSTCC 编译出来的原生二进制），不能跨平台/跨机器
# 复用——之前误提交过一份 macOS/arm64 的 Mach-O 到仓库里，在 Linux CI 上
# 被当成 shell 脚本执行导致 "word unexpected" 报错。用无扩展名匹配排除
# 掉，不排除同目录下的 mkfs.c 源文件。
labs/*/mkfs/mkfs

# 串口输出抓取：scripts/test-lab.sh 跑 QEMU 时的临时日志。
*.serial.log
serial-*.txt

# macOS
.DS_Store

# vscode files
.history/
.vscode/

tmp/